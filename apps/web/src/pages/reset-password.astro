---
const locale = (Astro.currentLocale ?? 'tr') as 'tr' | 'ar' | 'en' | 'fr' | 'es';
const token = Astro.url.searchParams.get('token') ?? undefined;
---
<PasswordResetPage locale={locale} token={token} />
